# ВАЖНО: комментарии только на отдельных строках — некоторые хостинги
# читают файл построчно и передают строку в pip как есть.

aiogram>=3.24,<4
aiosqlite>=0.20,<0.22
python-dotenv>=1.0,<2.0

# Веб-сервер мини-аппа. Приезжает и с aiogram, но здесь он используется
# напрямую, а не как деталь библиотеки: зависимость лучше назвать.
aiohttp>=3.9,<4

# Аккаунт для рассылки. Bot API войти по номеру не умеет в принципе —
# это MTProto. Версия важна: StringSession и разбор ошибок входа в
# старых сборках отличаются.
telethon>=1.41,<2.0

# Шифрование строк сессий в базе (Fernet). Telethon тянет её и сам, но
# сессии шифруются нашим кодом, поэтому зависимость указана явно.
cryptography>=42

# Разбор tdata — папки Telegram Desktop. Тянет за собой PyQt5: tdata
# написана сериализацией Qt, и читать её без Qt нечем. Из-за этого
# образ заметно тяжелеет, а на голом Debian нужна ещё системная
# libglib2.0-0 — она ставится в Dockerfile.
#
# Импортируется лениво: без неё отваливается только загрузка tdata,
# а бот и вход по номеру работают как ни в чём не бывало.
opentele>=1.15,<2

# Классификатор радара: отличить заказ от саморекламы исполнителя
# регуляркой нельзя — эти сообщения написаны одними и теми же словами.
# Без ключа радар поднимается и работает одним предфильтром, поэтому
# зависимость мягкая по смыслу, но нужна всегда: модуль импортируется
# на старте.
anthropic>=0.40
