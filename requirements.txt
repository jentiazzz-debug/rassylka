# ВАЖНО: комментарии только на отдельных строках — некоторые хостинги
# читают файл построчно и передают строку в pip как есть.

aiogram>=3.24,<4
aiosqlite>=0.20,<0.22
python-dotenv>=1.0,<2.0

# Веб-сервер мини-аппа. Приезжает и с aiogram, но здесь он используется
# напрямую, а не как деталь библиотеки: зависимость лучше назвать.
aiohttp>=3.9,<4

# Аккаунт для рассылки. Bot API войти по номеру не умеет в принципе —
# это MTProto. Версия важна: StringSession и разбор ошибок входа в
# старых сборках отличаются.
telethon>=1.41,<2.0

# Шифрование строк сессий в базе (Fernet). Telethon тянет её и сам, но
# сессии шифруются нашим кодом, поэтому зависимость указана явно.
cryptography>=42
